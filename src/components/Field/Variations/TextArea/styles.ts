import styled from "styled-components"

export const TextArea = styled.textarea`
  border: none;
  outline: none;
  font-size: 16px;
  flex: 1;
  min-width: unset;
  width: 100%;
  resize: vertical;
`
